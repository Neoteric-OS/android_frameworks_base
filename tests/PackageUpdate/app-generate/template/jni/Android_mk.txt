LOCAL_PATH := $(call my-dir)

include $(CLEAR_VARS)

LOCAL_MODULE    := lib%(libname)s
LOCAL_SRC_FILES := %(libname)s.c

LOCAL_CFLAGS += -Wall -Werror

LOCAL_MODULE_TAGS := tests

LOCAL_SDK_VERSION := 26

include $(BUILD_SHARED_LIBRARY)
